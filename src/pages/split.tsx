import PageLayout from "./components/PageLayout";
import { api } from "~/utils/api";

const Split = () => {
  return (
    <PageLayout>
      <div></div>
    </PageLayout>
  );
};

export default Split;
